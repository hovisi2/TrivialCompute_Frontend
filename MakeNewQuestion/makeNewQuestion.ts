function handleAddQuestion() {
    //add question to db 

    //if successful adding to db clear out test fields
    //else handle error
  }

  function handleHome(): void {
    window.location.href = "../HomePage/homePage.html";
  }
  